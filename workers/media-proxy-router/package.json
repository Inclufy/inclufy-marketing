{
  "name": "media-proxy-router",
  "version": "1.0.0",
  "description": "Cloudflare Worker that reverse-proxies images.inclufy.com to the Supabase media-proxy edge function",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.95.0"
  }
}
